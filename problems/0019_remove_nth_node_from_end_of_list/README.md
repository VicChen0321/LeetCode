# 19. Remove Nth Node From End of List

**LeetCode Link:**
[19. Remove Nth Node From End of List](https://leetcode.com/problems/remove-nth-node-from-end-of-list/)

**Difficulty:** Medium
**Tags:** Linked List, Two Pointers

---

## 題目描述

給定一個 **單向鏈結串列 head**，刪除 **從尾數第 n 個節點**，並回傳修改後的 head。

---

### 範例

**Input**

```
head = [1,2,3,4,5], n = 2
```

**Output**

```
[1,2,3,5]
```

**解釋**
尾數第 2 個 node 是 `4`，刪除它後變成 `[1,2,3,5]`。

---

**Input**

```
head = [1], n = 1
```

**Output**

```
[]
```

---

**Input**

```
head = [1,2], n = 1
```

**Output**

```
[1]
```

---

## 解法概念

### 🧠 方法：Two Pointers + Dummy Node

利用快慢指針技巧：

1. 建立一個 `dummy` 節點指向 `head`，避免刪除頭節點時不方便處理。
2. `fast` 指針先往前走 `n` 步。
3. 再讓 `slow` 和 `fast` 一起走，直到 `fast` 到達尾端。
4. 此時 `slow.next` 即為要刪除的節點。
5. 直接 `slow.next = slow.next.next` 完成刪除。

---

## 核心關鍵

✔ `dummy` 避免刪除頭節點時特例處理
✔ 使用 `fast` 先走 `n` 步製造距離
✔ 接著 `fast` 和 `slow` 一起走
✔ `slow` 到達 **欲刪除點前一個節點**

---

## 時間與空間複雜度

| 項目        | 複雜度  |
| --------- | ---- |
| **時間複雜度** | O(L) |
| **空間複雜度** | O(1) |

其中 L 為鏈結串列長度。
